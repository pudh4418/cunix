作业7B：

pspt.{c,h} 为本次作业的主要源代码。

Usage:
  t4 -s [-o output] 

-s 选项表示执行本次作业要求内容

此外，为了编程实现方便，本次作业的输入文件名被硬编码为 "data.txt"，其记录个数为 1000
输入文件可由 `gen.sh` 脚本生成

运行举例：

```
$ ./gen.sh > data.txt
$ ./t3 -s -o d.dat
```

